{% set version = "0.5.dev" %}

package:
  name: tvm
  version: {{ version }}

source:
  path: ../..

build:
  number: 0

requirements:
  build:
<<<<<<< HEAD:python/conda/meta.yaml
    - llvmdev ==4.0.0
    - cmake
    - python >=3
=======
    - {{ compiler('cxx') }}
  host:
    - python {{ python }}
    - cython
>>>>>>> 5e66870b31e16da7d0e95e5b0b4fc50d7cd02199:conda/tvm/meta.yaml
    - numpy
    - setuptools
    - decorator
    - tvm-libs =={{ version }}
  run:
    - python
    - {{ pin_compatible('numpy') }}
    - decorator
    - tvm-libs =={{ version }}

test:
  imports:
    - tvm

about:
  home: https://github.com/dmlc/tvm
  license: Apache2
  summary: a low level domain specific language for compiling tensor computation pipelines
